# Base completa (todas as colunas) — não é necessária para rodar o painel,
# apenas para análises avançadas. Fica de fora por causa do tamanho (~512 MB).
empresas_rs.parquet

# Lixo de sistema
Thumbs.db
desktop.ini
.DS_Store
